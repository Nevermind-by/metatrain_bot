[project]
name = "metatrain-bot"
version = "0.1.0"
description = "Telegram bot  for calories tracking, nutriotion management and workout progress."
readme = "README.md"
requires-python = ">=3.14"
dependencies = []
